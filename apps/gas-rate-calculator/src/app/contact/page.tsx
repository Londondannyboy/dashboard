import { ContactPage } from '@quest/ui/pages'

export const metadata = {
  title: 'Contact | Gas Rate Calculator',
}

export default function Contact() {
  return <ContactPage brandName="Gas Rate Calculator" />
}
