import { AboutPage } from '@quest/ui/pages'

export const metadata = {
  title: 'About | Gas Rate Calculator',
}

export default function About() {
  return <AboutPage brandName="Gas Rate Calculator" />
}
