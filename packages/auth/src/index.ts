// Re-export Stack Auth components for use in apps
export {
  StackProvider,
  StackTheme,
  UserButton,
  SignIn,
  SignUp,
  CredentialSignIn,
  CredentialSignUp,
  OAuthButtonGroup,
  MagicLinkSignIn,
} from '@stackframe/stack'

// Re-export types
export type { CurrentUser } from '@stackframe/stack'
